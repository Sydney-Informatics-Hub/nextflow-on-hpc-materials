params.pbspro_account = ""

process {
  executor = 'pbspro'
  queue = 'normal'
  clusterOptions = "-P ${params.pbspro_account}"
  module = 'singularity'
}

singularity {
  enabled = true
  autoMounts = true
  cacheDir = "${projectDir}/singularity" 
}
